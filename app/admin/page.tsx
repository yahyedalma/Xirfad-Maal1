'use client';
import './admin.css';

import { FormEvent, useEffect, useState } from 'react';
import { Award, BarChart3, BookOpen, FolderOpen, LayoutDashboard, LogOut, Plus, Settings, Users, UserRound, Video, Globe2 } from 'lucide-react';
import { useRouter } from 'next/navigation';
import { createClient } from '@supabase/supabase-js';

type Course={id:string;title:string;description:string|null;original_price:number|null;discounted_price:number|null};
export default function AdminPage(){const router=useRouter();const[courses,setCourses]=useState<Course[]>([]);const[counts,setCounts]=useState({users:0,enrollments:0,certificates:0});const[authorized,setAuthorized]=useState<boolean|null>(null);const[title,setTitle]=useState('');const[description,setDescription]=useState('');const[price,setPrice]=useState('0');const[discount,setDiscount]=useState('0');const[saving,setSaving]=useState(false);const[error,setError]=useState('');
 const supabase=()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);async function load(){const sb=supabase();const{data:auth}=await sb.auth.getUser();if(!auth.user){router.replace('/login');return;}const{data:p}=await sb.from('profiles').select('role').eq('id',auth.user.id).single();const ok=p?.role==='ADMIN'||p?.role==='OWNER';setAuthorized(ok);if(!ok)return;const[{data:c},{count:users},{count:enrollments},{count:certificates}]=await Promise.all([sb.from('courses').select('id,title,description,original_price,discounted_price').order('created_at',{ascending:false}),sb.from('profiles').select('id',{count:'exact',head:true}),sb.from('enrollments').select('id',{count:'exact',head:true}),sb.from('certificates').select('id',{count:'exact',head:true})]);setCourses(c??[]);setCounts({users:users??0,enrollments:enrollments??0,certificates:certificates??0});}useEffect(()=>{load();},[]);
 async function addCourse(e:FormEvent){e.preventDefault();setSaving(true);setError('');const{error:insertError}=await supabase().from('courses').insert({title:title.trim(),description:description.trim(),original_price:Number(price)||0,discounted_price:Number(discount)||0});if(insertError)setError(insertError.message);else{setTitle('');setDescription('');setPrice('0');setDiscount('0');await load();}setSaving(false);}async function logout(){await supabase().auth.signOut();router.replace('/');}
 if(authorized===null)return <main className="admin-loading"><div className="admin-loader">Hubinaya maamulka...</div></main>;if(!authorized)return <main className="section"><div className="container"><div className="hero-card"><h2>Maamulka waa xiran yahay</h2><p className="muted">Akoonkan ma laha ADMIN ama OWNER role.</p><a className="btn btn-primary" href="/dashboard">Ku noqo dashboard</a></div></div></main>;
 const menu=[['Dashboard',LayoutDashboard,'/admin'],['Website Editor',Globe2,'/admin/website'],['Courses',BookOpen,'#courses'],['Categories',FolderOpen,'#categories'],['Students',Users,'#students'],['Instructors',UserRound,'#instructors'],['Enrollments',BarChart3,'#enrollments'],['Lessons',Video,'#lessons'],['Certificates',Award,'#certificates'],['Settings',Settings,'#settings']] as const;
 return <main className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand"><span className="brand-mark">✦</span><span><b>Xirfad Maal</b><small>ACADEMY</small></span></div><div className="admin-menu">{menu.map(([label,Icon,href],i)=><a className={i===0?'selected':''} href={href} key={label}><Icon size={16}/>{label}</a>)}</div><button className="admin-logout" onClick={logout}><LogOut size={16}/> Logout</button></aside><section className="admin-main"><header className="admin-top"><div><span className="section-kicker">ADMIN CONTROL CENTER</span><h1>Admin Dashboard</h1><p>Welcome back, Admin 👋</p></div><a className="btn btn-primary" href="/"><Globe2 size={16}/> Arag Website-ka</a></header><div className="dash-stats admin-stats"><div className="feature"><Users/><small>Arday Guud</small><h3>{counts.users}</h3><b>↗ Active users</b></div><div className="feature"><BookOpen/><small>Courses</small><h3>{courses.length}</h3><b>↗ Live courses</b></div><div className="feature"><UserRound/><small>Instructors</small><h3>20+</h3><b>↗ Teaching team</b></div><div className="feature"><Award/><small>Certificates</small><h3>{counts.certificates}</h3><b>↗ Issued</b></div></div><div className="admin-grid"><section className="admin-panel"><div className="panel-head"><div><h2>Ku dar Course</h2><p>Course cusub ku geli database-ka.</p></div><Plus size={20}/></div><form onSubmit={addCourse} className="admin-form"><input placeholder="Magaca koorsada" required value={title} onChange={e=>setTitle(e.target.value)}/><textarea placeholder="Sharaxaad kooban" rows={4} value={description} onChange={e=>setDescription(e.target.value)}/><div className="form-two"><input type="number" min="0" placeholder="Qiimaha" value={price} onChange={e=>setPrice(e.target.value)}/><input type="number" min="0" placeholder="Qiimaha dhimista" value={discount} onChange={e=>setDiscount(e.target.value)}/></div>{error&&<p className="form-error">{error}</p>}<button className="btn btn-primary" disabled={saving}>{saving?'Kaydinaya...':'Publish Course'}</button></form></section><section className="admin-panel"><div className="panel-head"><div><h2>Activity Overview</h2><p>Enrollment trend</p></div><span className="chart-pill">Last 7 months</span></div><div className="fake-chart"><div className="chart-line"><span/><span/><span/><span/><span/><span/><span/></div><div className="chart-axis"><small>Jan</small><small>Feb</small><small>Mar</small><small>Apr</small><small>May</small><small>Jun</small><small>Jul</small></div></div><div className="enroll-total">{counts.enrollments}<span>Total enrollments</span></div></section></div><section id="courses" className="admin-panel courses-admin"><div className="panel-head"><div><h2>Courses</h2><p>Koorsooyinka ku jira database-ka.</p></div><a className="btn btn-primary" href="#courses">Manage Courses</a></div><div className="admin-course-list">{courses.map(c=><article key={c.id}><div className="admin-course-icon">{c.title.slice(0,2).toUpperCase()}</div><div><b>{c.title}</b><span>{c.description||'Sharaxaad lama gelin.'}</span></div><strong>{Number(c.discounted_price??0)===0?'Free':`$${c.discounted_price}`}</strong><a className="btn btn-outline" href={`/courses/${c.id}`}>View</a></article>)}</div></section></section></main>}
